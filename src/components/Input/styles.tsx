import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputer: {
    backgroundColor: '#e5bf3c',
    width: '100%',
    fontSize: 20,
    lineHeight: 20,
    color: '#000',
    height: 40,
    borderRadius: 5,
    padding: 10,
    textAlign: 'center'
  }
});