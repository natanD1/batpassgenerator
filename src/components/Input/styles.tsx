import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputer: {
    backgroundColor: '#e18e3b',
    width:'100%',
    fontSize: 20,
    color: '#000',
    height: 40,
    borderWidth: 1,
    borderRadius: 5,
    padding: 10,
}
});